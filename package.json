{
    "scripts": {
        "dev": "wrangler dev --node-compat --live-reload src/index.ts",
        "deploy": "wrangler deploy --minify src/index.ts"
    },
    "dependencies": {
        "hono": "^3.7.0"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20230821.0",
        "@types/node": "^20.6.3",
        "wrangler": "^3.7.0"
    }
}
